config.site=Site configuration
config.site.login_path=Login page path
config.site.register_url=Register pagepath
config.site.allowed_origins=Cors allowed origins
config.site.allowed_origins.description=Set the origins to allow,The special value "*" allows all domains.
config.site.allowed_methods=Cors allowed methods
config.site.allowed_methods.description=Set the HTTP methods to allow,The special value "*" allows all methods.
config.site.allowed_headers=Cors allowed headers
config.site.allowed_headers.description=Set the list of headers that a pre-flight request can list as allowed  for use during an actual request.The special value "*" allows actual requests to send any header.
config.site.exposed_headers=Cors exposed headers
config.site.exposed_headers.description=Set the list of response headers other than simple headers.
config.site.allow_credentials=Cors allow credentials
config.site.allow_credentials.description=Whether user credentials are supported.
config.site.max_age=Cors max age
config.site.max_age.description=Configure how long, in seconds, the response from a pre-flight request can be cached by clients.
config.siteAttribute=Site extend attribute configuration,Can be used for any template
config.siteAttribute.logo=Site logo
config.siteAttribute.logo.description=Recommended image size width 200px height 69px
config.email=Mail configuration
config.email.email_title=Mail title
config.email.email_path=Mail template path
config.email.defaultEncoding=Default coding
config.email.host=Smtp server address
config.email.port=Port
config.email.username=Login account
config.email.password=Login password
config.email.timeout=Timeout
config.email.auth=Need login server
config.email.ssl=Use SSL
config.email.fromAddress=Send email address